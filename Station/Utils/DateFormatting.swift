import Foundation

/*
 Station > Utils > DateFormatting.swift
 --------------------------------------
 PURPOSE:
 This file is currently a placeholder.
 In the future, shared date formatters (like "MMM d, yyyy") could be moved here
 to avoid creating new `DateFormatter()` objects every time we need them, which is expensive.
 */

// Date formatting utilities
